# Implementation

## Summary

- Set default project column width to `minProjectColumnWidth`.
- Set default detail column width to `minDetailColumnWidth`.
- Versioned localStorage keys to `.v2` so previous wider defaults do not carry forward.

## Files Changed

- `src/renderer/App.tsx`
