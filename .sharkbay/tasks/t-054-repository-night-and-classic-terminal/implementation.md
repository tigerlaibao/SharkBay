# t-054-repository-night-and-classic-terminal Implementation

## Changes

- Added Night theme coverage for `.fact` tiles used by the Repository panel.
- Added Night text/warn colors for Repository fact labels, values, and warning values.
- Added Classic terminal parity touches for terminal panel shadow, terminal header title color, and exited status dot color.

## Notes

- Classic still uses the old xterm core palette from before T051: `#101719` background, `#d9e5df` foreground, `#93d7a4` cursor, and `#38575d` selection.
